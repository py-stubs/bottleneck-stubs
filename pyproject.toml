[project]
name = "bn-typed"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.2.6",
]
[tool.setuptools.package-data]
"src" = ["py.typed"]
